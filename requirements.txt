# Runtime dependencies. For the exact versions used in the paper see
# requirements-lock.txt.
#
# PyTorch is intentionally unpinned here — install the build matching your CUDA
# version first, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

torch>=2.4
torchvision>=0.19
segmentation-models-pytorch>=0.4
timm>=1.0

numpy>=2.0
scipy>=1.13
pandas>=2.2
scikit-learn>=1.5
matplotlib>=3.9
tqdm>=4.66
tensorboard>=2.17

# Dataset generation (scripts/build_dataset.py, scripts/download_tartanground.py)
open3d>=0.18
opencv-contrib-python>=4.10
tartanair>=1.0
